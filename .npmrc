# PRD 4.1: pin exact versions in the lockfile.
save-exact=true
engine-strict=true

# PRD 4.1: no library ships because it is fashionable. Strict isolation makes an
# undeclared cross-package dependency a build failure rather than a silent hoist.
resolution-mode=highest
auto-install-peers=false
strict-peer-dependencies=false
